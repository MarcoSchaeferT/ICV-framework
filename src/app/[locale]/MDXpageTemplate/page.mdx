import MdxLayout from '@/mdx-layout'
import Home from '../worldView/page.tsx'


<MdxLayout>

# Test Content

This is a placeholder section test content. Add more details as needed.

## Formatting Examples

Use **bold**, _italics_, and `inline code` to emphasize concepts.
> Helpful tip: Keep MDX content focused and structured for readability.

- Highlight key takeaways with bullet lists.
- Mix Markdown syntax with JSX for rich interactivity.

==Highlight important text with double equals.==

```javascript
// Sample code block
function greet(name) {
  return `Hello, ${name}!`;
}
```

test ipsu test zi iem

</MdxLayout>
<div className="justify-center items-center flex min-h-screen px-4">
<Home/>


</div>
